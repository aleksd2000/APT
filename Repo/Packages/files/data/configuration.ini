# Setup Configuration File

# Maybe consider having a custom configuration file for each install method
# Obviouslt need to consider a method to be able to add to the list of dependencies without having to edit the source code of the program

# Adding arrays for all 3 depX variables - 20220118
    declare -a dep                   # dep array - app name
    declare -a depmethod             # depmethod array - Install method tool
    declare -a depsource             # depsource array - Install method source

# Custom arrays added by Aleks 
    declare -a depfilename           # depfilename array - Mod file name
    declare -a ppainstall   	     # ppainstall array - For Additional PPA Requirements

if [ "$HOSTNAME" = "DESKTOP-6413AM3" ]; then
    REPO="GIT:Repos/Complete/testing-aleks"
    CWD="$HOME/$REPO"

    source $CWD/files/data/configuration/$HOSTNAME.ini
    export networkingmodule=false
fi

if [ "$HOSTNAME" = "obsidian.aleksd2000.cc" ]; then
    REPO="Remote/Github/Complete/testing-aleks"
    CWD="$HOME/$REPO"

    source $CWD/files/data/configuration/$HOSTNAME.ini
    export networkingmodule=false
fi

#if [ "$HOSTNAME" = "obsidian.aleksd2000.cc" ]; then
#
#    CWD=$HOME/$REPO
#
#    source $CWD/files/data/configuration/instance-20220102-2354.ini
#    export networkingmodule=false
#fi

if [ "$HOSTNAME" = "renegade.aleksd2000.cc" ]; then
    CWD=$HOME/$REPO

    source $CWD/files/data/configuration/instance-20220102-2354.ini
    export networkingmodule=false
fi

if [ "$HOSTNAME" = "06b2318daac5" ]; then
    CWD=$HOME/$REPO/
    source files/data/configuration/$HOSTNAME.ini
    export networkingmodule=false
fi

if [ "$HOSTNAME" = "instance-20220102-2354" ]; then
    source files/data/configuration/$HOSTNAME.ini
    export networkingmodule=false
fi

if [ "$HOSTNAME" = "LAPTOP-NTBOV92O" ]; then
    source files/data/configuration/$HOSTNAME.ini
    export networkingmodule=false
fi

if [ "$HOSTNAME" = "backup2.aleksd2000.cc" ]; then
    source files/data/configuration/$HOSTNAME.ini
    export networkingmodule=false
fi

if [ "$HOSTNAME" = "recovery.linuxutopia.com" ]; then
    source files/data/configuration/$HOSTNAME.ini
    export networkingmodule=true
fi

# Final line needed to ensure dep_count actually works...

    dep_count=${#dep[*]}                # Total number of elements
