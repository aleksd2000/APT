# Module Configuration File

    # Module Name: git
    dep+=("git")                      
    depmethod+=("apt")
    depsource+=("/usr/bin/apt-get")
    depfilename+=("files/data/dependencies/hosts/$HOSTNAME/depXX.mod")
    ppainstall+=("")

