# Module Configuration File

    dep+=("rclone")
    depmethod+=("curl")
    depsource+=("/usr/bin/curl -fsSL https://rclone.org/install.sh | sudo bash -s beta")
    depfilename+=("files/data/dependencies/hosts/$HOSTNAME/dep08.mod")
    ppainstall+=("")

