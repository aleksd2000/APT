# Module Configuration File

    # Module Name: screen

    dep+=("screen")                   
    depmethod+=("apt")
    depsource+=("/usr/bin/apt-get install")
    depfilename+=("files/data/dependencies/hosts/$HOSTNAME/dep02.mod")
    ppainstall+=("")
